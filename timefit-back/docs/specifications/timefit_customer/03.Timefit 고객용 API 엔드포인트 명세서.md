# 03. Timefit 고객용 API 엔드포인트 명세서

## 📋 문서 정보
- **대상**: 고객용 API 개발
- **베이스 URL**: `/api`

---

## 🎯 API 설계 원칙

### 1. RESTful 원칙
```
GET - 조회
POST - 생성
PUT - 전체 수정
PATCH - 부분 수정
DELETE - 삭제
```

### 2. 경로 규칙
```
리소스명은 복수형 사용 (목록 반환 시)
- /api/reservations (목록)
- /api/reservation/{id} (단일)

서브 리소스
- /api/business/{id}/menu (메뉴 목록)
- /api/business/{id}/reviews (리뷰 목록)
```

### 3. 인증
```
필수: Authorization: Bearer {JWT_TOKEN}
불필요: 공개 API (검색, 업체 상세, 메뉴 조회 등)
```

### 4. 에러 코드
```
200 OK - 성공
201 Created - 생성 성공
204 No Content - 삭제 성공
400 Bad Request - 잘못된 요청
401 Unauthorized - 인증 필요
403 Forbidden - 권한 없음
404 Not Found - 리소스 없음
409 Conflict - 중복/충돌
```

---

## 📚 API 목록

### 범례
- ✅ 구현 완료
- 🔄 수정 필요
- 🆕 신규 개발

---

## 1. 👤 User API

### 1.1 현재 사용자 정보 조회 ✅
```
GET /api/user/me
```

**인증**: 필수  
**권한**: 모든 사용자  
**Response**: `CurrentUserResponse`

#### 응답 예시
```json
{
  "success": true,
  "data": {
    "userId": "uuid",
    "email": "user@example.com",
    "name": "홍길동",
    "phoneNumber": "01012345678",
    "profileImageUrl": "https://...",
    "createdAt": "2025-01-01T00:00:00",
    "lastLoginAt": "2025-01-30T10:00:00",
    "businesses": []
  }
}
```

---

### 1.2 프로필 조회 🆕
```
GET /api/user/profile
```

**인증**: 필수  
**권한**: 본인  
**Response**: `UserProfileResponse`

#### 응답 예시
```json
{
  "success": true,
  "data": {
    "userId": "uuid",
    "email": "user@example.com",
    "name": "홍길동",
    "phoneNumber": "01012345678",
    "profileImageUrl": "https://...",
    "createdAt": "2025-01-01T00:00:00",
    "lastLoginAt": "2025-01-30T10:00:00",
    "totalReservations": 10,
    "completedReservations": 8,
    "wishlistCount": 5,
    "reviewCount": 3
  }
}
```

---

### 1.3 프로필 수정 🆕
```
PUT /api/user/profile
```

**인증**: 필수  
**권한**: 본인  
**Request**: `UpdateProfileRequest`  
**Response**: `UserProfileResponse`

#### 요청 예시
```json
{
  "name": "김철수",
  "phoneNumber": "01087654321",
  "profileImageUrl": "https://..."
}
```

**참고**: 모든 필드 선택 (부분 수정 가능)

---

### 1.4 비밀번호 변경 🆕
```
POST /api/user/passwordchange
```

**인증**: 필수  
**권한**: 본인  
**Request**: `ChangePasswordRequest`  
**Response**: 200 OK

#### 요청 예시
```json
{
  "currentPassword": "OldPass123!",
  "newPassword": "NewPass123!",
  "newPasswordConfirm": "NewPass123!"
}
```

**검증**:
- 현재 비밀번호 일치 확인
- 새 비밀번호 8~20자, 대소문자+숫자+특수문자 포함
- 새 비밀번호 확인 일치

---

## 2. 🏢 Business API (공개)

### 2.1 업체 검색 ✅
```
GET /api/business/search
```

**인증**: 불필요  
**Query Parameters**:
- `keyword` (선택): 검색 키워드
- `businessType` (선택): 업종 코드 (BD000~BD013)
- `region` (선택): 지역
- `page` (선택): 페이지 번호 (기본: 0)
- `size` (선택): 페이지 크기 (기본: 20)

**Response**: `BusinessListResponse`

#### 요청 예시
```
GET /api/business/search?keyword=헤어샵&region=강남&page=0&size=20
```

#### 응답 예시
```json
{
  "success": true,
  "data": {
    "businesses": [
      {
        "businessId": "uuid",
        "businessName": "강남 헤어샵",
        "businessType": ["BD007"],
        "address": "서울시 강남구...",
        "averageRating": 4.5,
        "reviewCount": 120,
        "logoUrl": "https://..."
      }
    ],
    "totalCount": 50
  }
}
```

---

### 2.2 업체 상세 조회 (공개) 🔄
```
GET /api/business/{businessId}
```

**인증**: 불필요  
**Response**: `PublicBusinessResponse`

**수정 필요**:
- `averageRating` 필드 추가
- `reviewCount` 필드 추가
- `businessNotice` 필드 추가 (고객용 공지)

#### 응답 예시
```json
{
  "success": true,
  "data": {
    "businessId": "uuid",
    "businessName": "강남 헤어샵",
    "businessTypes": ["BD007"],
    "address": "서울시 강남구 강남대로 123",
    "contactPhone": "02-1234-5678",
    "description": "최고의 헤어샵입니다",
    "businessNotice": "주차 가능합니다",
    "averageRating": 4.5,
    "reviewCount": 120,
    "logoUrl": "https://...",
    "isActive": true
  }
}
```

---

### 2.3 업체 상세 조회 (업체용) ✅
```
GET /api/business/{businessId}/profile
```

**인증**: 필수  
**권한**: 해당 업체 구성원  
**Response**: `BusinessResponse`

**참고**: 사업자번호, 내부 공지 등 민감 정보 포함

---

## 3. 🍽️ Menu API (공개)

### 3.1 메뉴 목록 조회 ✅
```
GET /api/business/{businessId}/menu
```

**인증**: 불필요  
**Query Parameters**:
- `serviceName` (선택): 서비스명 검색
- `businessCategoryId` (선택): 카테고리 ID
- `minPrice` (선택): 최소 가격
- `maxPrice` (선택): 최대 가격
- `isActive` (선택): 활성 상태 (기본: true)

**Response**: `MenuList`

#### 요청 예시
```
GET /api/business/{businessId}/menu?minPrice=10000&maxPrice=50000
```

#### 응답 예시
```json
{
  "success": true,
  "data": {
    "menus": [
      {
        "menuId": "uuid",
        "serviceName": "헤어 컷",
        "price": 30000,
        "durationMinutes": 60,
        "orderType": "RESERVATION_BASED",
        "imageUrl": "https://...",
        "isActive": true
      }
    ],
    "totalCount": 10
  }
}
```

---

### 3.2 메뉴 상세 조회 ✅
```
GET /api/business/{businessId}/menu/{menuId}
```

**인증**: 불필요  
**Response**: `Menu`

#### 응답 예시
```json
{
  "success": true,
  "data": {
    "menuId": "uuid",
    "businessId": "uuid",
    "serviceName": "헤어 컷",
    "businessCategoryId": "uuid",
    "businessType": "BD007",
    "categoryName": "헤어",
    "price": 30000,
    "description": "기본 헤어 컷 서비스",
    "orderType": "RESERVATION_BASED",
    "durationMinutes": 60,
    "imageUrl": "https://...",
    "isActive": true,
    "createdAt": "2025-01-01T00:00:00",
    "updatedAt": "2025-01-30T10:00:00"
  }
}
```

---

## 4. 📅 Reservation API

### 4.1 예약 생성 ✅
```
POST /api/reservation
```

**인증**: 필수  
**권한**: 고객  
**Request**: `CreateReservationRequest`  
**Response**: `CustomerReservation`

#### 요청 예시
```json
{
  "businessId": "uuid",
  "menuId": "uuid",
  "bookingSlotId": "uuid",
  "durationMinutes": 60,
  "totalPrice": 30000,
  "customerName": "홍길동",
  "customerPhone": "01012345678",
  "notes": "창가 자리 부탁드립니다"
}
```

---

### 4.2 내 예약 목록 조회 ✅
```
GET /api/reservations
```

**인증**: 필수  
**권한**: 본인  
**Query Parameters**:
- `status` (선택): 예약 상태 필터
- `startDate` (선택): 시작 날짜 (YYYY-MM-DD)
- `endDate` (선택): 종료 날짜 (YYYY-MM-DD)
- `businessId` (선택): 업체 ID 필터
- `page` (선택): 페이지 번호 (기본: 0)
- `size` (선택): 페이지 크기 (기본: 20)

**Response**: `CustomerReservationList`

#### 요청 예시
```
GET /api/reservations?status=CONFIRMED&page=0&size=20
```

#### 응답 예시
```json
{
  "success": true,
  "data": {
    "summary": {
      "upcomingCount": 3,
      "completedCount": 10,
      "cancelledCount": 1
    },
    "reservations": [
      {
        "reservationId": "uuid",
        "reservationNumber": "R20251123001",
        "status": "CONFIRMED",
        "businessName": "강남 헤어샵",
        "menuServiceName": "헤어 컷",
        "reservationDate": "2025-02-01",
        "reservationTime": "14:00:00",
        "reservationPrice": 30000
      }
    ],
    "totalCount": 14
  }
}
```

---

### 4.3 예약 상세 조회 ✅
```
GET /api/reservation/{reservationId}
```

**인증**: 필수  
**권한**: 본인  
**Response**: `CustomerReservation`

---

### 4.4 예약 수정 ✅
```
PUT /api/reservation/{reservationId}
```

**인증**: 필수  
**권한**: 본인  
**Request**: `UpdateReservationRequest`  
**Response**: `CustomerReservation`

**제약**:
- PENDING 상태에서만 수정 가능
- 예약 시간 48시간 전까지만 가능

---

### 4.5 예약 취소 ✅
```
DELETE /api/reservation/{reservationId}
```

**인증**: 필수  
**권한**: 본인  
**Response**: 204 No Content

**제약**:
- PENDING, CONFIRMED 상태에서만 취소 가능
- 예약 시간 24시간 전까지만 가능

---

## 5. ⭐ Wishlist API

### 5.1 찜 추가 🆕
```
POST /api/user/wishlist
```

**인증**: 필수  
**권한**: 본인  
**Request**: `AddWishlistRequest`  
**Response**: `WishlistActionResponse`

#### 요청 예시
```json
{
  "menuId": "uuid"
}
```

#### 응답 예시
```json
{
  "success": true,
  "data": {
    "menuId": "uuid",
    "isWishlisted": true,
    "message": "찜 목록에 추가되었습니다"
  }
}
```

**에러**:
- 409 Conflict: 이미 찜한 메뉴

---

### 5.2 찜 삭제 🆕
```
DELETE /api/user/wishlist/{menuId}
```

**인증**: 필수  
**권한**: 본인  
**Response**: `WishlistActionResponse`

#### 응답 예시
```json
{
  "success": true,
  "data": {
    "menuId": "uuid",
    "isWishlisted": false,
    "message": "찜 목록에서 제거되었습니다"
  }
}
```

---

### 5.3 찜 목록 조회 🆕
```
GET /api/user/wishlists
```

**인증**: 필수  
**권한**: 본인  
**Query Parameters**:
- `page` (선택): 페이지 번호 (기본: 0)
- `size` (선택): 페이지 크기 (기본: 20)

**Response**: `WishlistListResponse`

#### 응답 예시
```json
{
  "success": true,
  "data": {
    "wishlists": [
      {
        "wishlistId": "uuid",
        "menuId": "uuid",
        "serviceName": "헤어 컷",
        "price": 30000,
        "durationMinutes": 60,
        "imageUrl": "https://...",
        "businessId": "uuid",
        "businessName": "강남 헤어샵",
        "createdAt": "2025-01-20T10:00:00"
      }
    ],
    "totalCount": 5
  }
}
```

---

## 6. 📝 Review API

### 6.1 리뷰 작성 🆕
```
POST /api/review
```

**인증**: 필수  
**권한**: 본인 (예약 완료한 고객)  
**Request**: `CreateReviewRequest`  
**Response**: `ReviewResponse`

#### 요청 예시
```json
{
  "reservationId": "uuid",
  "rating": 5,
  "comment": "정말 만족스러운 서비스였습니다!"
}
```

**제약**:
- 예약 상태가 COMPLETED여야 함
- 1개 예약당 1개 리뷰만 작성 가능

---

### 6.2 리뷰 수정 🆕
```
PUT /api/review/{reviewId}
```

**인증**: 필수  
**권한**: 본인  
**Request**: `UpdateReviewRequest`  
**Response**: `ReviewResponse`

#### 요청 예시
```json
{
  "rating": 4,
  "comment": "수정된 리뷰 내용입니다"
}
```

---

### 6.3 리뷰 삭제 🆕
```
DELETE /api/review/{reviewId}
```

**인증**: 필수  
**권한**: 본인  
**Response**: 204 No Content

**참고**: Soft Delete (deleted_at 업데이트)

---

### 6.4 업체별 리뷰 목록 조회 🆕
```
GET /api/business/{businessId}/reviews
```

**인증**: 불필요  
**Query Parameters**:
- `rating` (선택): 평점 필터 (1~5)
- `page` (선택): 페이지 번호 (기본: 0)
- `size` (선택): 페이지 크기 (기본: 20)
- `sort` (선택): 정렬 (latest, rating_high, rating_low)

**Response**: `ReviewListResponse`

#### 요청 예시
```
GET /api/business/{businessId}/reviews?rating=5&page=0&size=20&sort=latest
```

#### 응답 예시
```json
{
  "success": true,
  "data": {
    "statistics": {
      "averageRating": 4.5,
      "totalReviews": 120,
      "ratingDistribution": {
        "5": 80,
        "4": 30,
        "3": 8,
        "2": 1,
        "1": 1
      }
    },
    "reviews": [
      {
        "reviewId": "uuid",
        "userId": "uuid",
        "userName": "홍길동",
        "userProfileImage": "https://...",
        "menuName": "헤어 컷",
        "rating": 5,
        "comment": "정말 만족스러웠습니다!",
        "createdAt": "2025-01-30T10:00:00"
      }
    ],
    "totalCount": 80,
    "pagination": {
      "currentPage": 0,
      "pageSize": 20,
      "totalPages": 4,
      "totalElements": 80
    }
  }
}
```

---

### 6.5 내가 작성한 리뷰 목록 🆕
```
GET /api/user/reviews
```

**인증**: 필수  
**권한**: 본인  
**Query Parameters**:
- `page` (선택): 페이지 번호 (기본: 0)
- `size` (선택): 페이지 크기 (기본: 20)

**Response**: `MyReviewListResponse`

#### 응답 예시
```json
{
  "success": true,
  "data": {
    "reviews": [
      {
        "reviewId": "uuid",
        "businessId": "uuid",
        "businessName": "강남 헤어샵",
        "menuName": "헤어 컷",
        "rating": 5,
        "comment": "정말 만족스러웠습니다!",
        "createdAt": "2025-01-30T10:00:00"
      }
    ],
    "totalCount": 3
  }
}
```

---

## 7. 📍 BookingSlot API (공개)

### 7.1 예약 가능 시간 조회 ✅
```
GET /api/business/{businessId}/booking-slot/menu/{menuId}
```

**인증**: 불필요  
**Query Parameters**:
- `startDate`: 조회 시작 날짜 (필수, YYYY-MM-DD)
- `endDate`: 조회 종료 날짜 (필수, YYYY-MM-DD)

**Response**: `BookingSlotList`

#### 요청 예시
```
GET /api/business/{businessId}/booking-slot/menu/{menuId}?startDate=2025-02-01&endDate=2025-02-07
```

---


## 🔒 권한 매트릭스

| API | 인증 | 권한 | 비고 |
|-----|------|------|------|
| GET /api/business/search | ❌ | 공개 | |
| GET /api/business/{id} | ❌ | 공개 | |
| GET /api/business/{id}/menu | ❌ | 공개 | |
| GET /api/business/{id}/reviews | ❌ | 공개 | |
| GET /api/user/me | ✅ | 본인 | |
| GET /api/user/profile | ✅ | 본인 | |
| PUT /api/user/profile | ✅ | 본인 | |
| POST /api/user/passwordchange | ✅ | 본인 | |
| POST /api/reservation | ✅ | 고객 | |
| GET /api/reservations | ✅ | 본인 | |
| POST /api/user/wishlist | ✅ | 본인 | |
| GET /api/user/wishlists | ✅ | 본인 | |
| POST /api/review | ✅ | 본인 | COMPLETED 예약 필요 |
| GET /api/user/reviews | ✅ | 본인 | |

---

## 📝 공통 에러 응답

### 400 Bad Request
```json
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "입력값이 올바르지 않습니다",
    "details": [
      "rating은 1~5 사이여야 합니다"
    ]
  }
}
```

### 401 Unauthorized
```json
{
  "success": false,
  "error": {
    "code": "UNAUTHORIZED",
    "message": "인증이 필요합니다"
  }
}
```

### 403 Forbidden
```json
{
  "success": false,
  "error": {
    "code": "FORBIDDEN",
    "message": "권한이 없습니다"
  }
}
```

### 404 Not Found
```json
{
  "success": false,
  "error": {
    "code": "RESOURCE_NOT_FOUND",
    "message": "요청한 리소스를 찾을 수 없습니다"
  }
}
```

### 409 Conflict
```json
{
  "success": false,
  "error": {
    "code": "ALREADY_EXISTS",
    "message": "이미 존재하는 리소스입니다"
  }
}
```

---

## 📌 개발 참고사항

### 1. 기존 API 활용
- Business 검색/상세: 이미 구현됨
- Menu 조회: 이미 구현됨
- Reservation CRUD: 이미 구현됨
- BookingSlot 조회: 이미 구현됨

### 2. 신규 개발 필요
- User 프로필 수정/비밀번호 변경
- Wishlist 전체 (추가/삭제/목록)
- Review 전체 (작성/수정/삭제/목록)

### 3. 수정 필요
- PublicBusinessResponse: averageRating, reviewCount 필드 추가

### 4. Controller 구조
```
기존 활용:
- BusinessController (공개 API)
- MenuController (공개 API)
- ReservationController (고객 API)

신규 생성:
- WishlistController (/api/user/wishlist)
- ReviewController (/api/review)
- UserProfileController (/api/user/profile, /api/user/passwordchange)
```