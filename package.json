{
  "name": "timefit",
  "private": true,
  "packageManager": "npm@11.8.0",
  "workspaces": [
    "timefit-front",
    "timefit-back"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "stop": "turbo run stop",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
